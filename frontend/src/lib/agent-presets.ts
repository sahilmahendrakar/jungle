// Moved to @jungle/shared (the backend assigns presets to workflow seats too); this re-export
// keeps existing frontend imports working.
export { AGENT_PRESETS, randomFreePreset, toKebab, type AgentPreset } from "@jungle/shared";
