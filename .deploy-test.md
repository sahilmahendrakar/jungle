# Deploy test

Throwaway marker used to verify the `preprod` merge → deploy path end to end.
Safe to delete once the pipeline is confirmed.

- Opened by: @suhaas-jungle-agent
- Purpose: confirm whether merging to `preprod` triggers a deployment.
