# Copy to .env (or run `node scripts/dev-setup.mjs` to create it automatically).
DATABASE_URL=postgres://jungle:jungle@127.0.0.1:5432/jungle

# Only needed for agent features (run `node scripts/smoke.mjs` once to create .jungle-ids.json).
# ANTHROPIC_API_KEY=
